{
  "name": "stock-market-sim-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "path": "^0.12.7",
    "pg": "^8.7.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^2.0.15",
    "supertest": "^7.0.0"
  }
}
